# Archived experiments

`legacy_score_pipeline/` preserves the earlier weighted-score / GO-CONDITIONAL-NO-GO implementation, its tests, source-derived candidates, calibration reports, and design documents. It is intentionally outside the import path, test suite, runtime data, and public workflow.

It is retained for research provenance only. Nothing under `tbd/` should be interpreted as current evalAB behaviour or copied into a production decision rule without a separate review.
